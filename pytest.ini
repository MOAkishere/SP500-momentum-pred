[pytest]
# No automated tests exist yet — this project is currently exploratory notebooks.
# When tests are added, place them under a tests/ directory (test_*.py) and they
# will be discovered automatically.
addopts = -ra
